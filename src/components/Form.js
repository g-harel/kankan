import React, { Component } from 'react';

class Form extends Component {
    render() {
        <div>
            {this.props.map(() => {
                
            })}
        </div>
    }
}

export default Form;
